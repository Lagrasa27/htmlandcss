// diference between null and undefined. null is non-instentional log of value but undefined isn't

// let age;

// console.log(age, age + 3, `the age is ${age}`);


let age = null;

console.log(age, age + 3, `the age is ${age}`);
