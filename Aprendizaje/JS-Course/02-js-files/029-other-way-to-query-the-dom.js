// get an element by ID
const section = document.getElementById('dog-man');
console.log(section);

// get elements by theri class name

const ken = document.getElementsByClassName('error');
console.log(ken );
console.log(ken[0])

// get elements by theri tag name

const paras = document.getElementsByTagName('p');
console.log(paras);
console.log(paras[1]);

// if you are using one of the method you can not usea method foraach
// which is  an advantage of using queryselector
